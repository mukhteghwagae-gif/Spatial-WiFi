This directory contains Room database schema JSON files exported at compile-time.
These files are version-controlled so that:
  • Database migrations can be verified automatically.
  • Schema history is auditable via git log.

Do NOT delete or manually edit these files.
They are regenerated by KSP whenever the Room schema version changes.
